[package]
name = "nbdkit-erofs-plugin"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
libc = "0.2"
zerocopy = { version = "0.8", features = ["derive"] }
crc32fast = "1.4"
